{
  "name": "first-impressions",
  "version": "0.1.0",
  "private": true,
  "description": "CLI market-reaction simulator powered by persona-based AI subagents.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "bin": {
    "first-impressions": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
